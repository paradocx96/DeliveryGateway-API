apiVersion: v1
kind: Service
metadata:
  name: delivery-gateway-api-service
spec:
  type: NodePort
  selector:
    app: delivery-gateway-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30100
